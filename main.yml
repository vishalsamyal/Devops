- name: check all variables
  assert:
    that:
     - mysql_version       is defined
     - root_db_password     is defined

- name: Disable firewalld
  service:
   name=firewalld
   state=stopped
   enabled=no
   
 - name: add user
  include_tasks: mysql.yml